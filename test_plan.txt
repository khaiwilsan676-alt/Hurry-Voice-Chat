1. In `app/page.tsx`, we need to check if the user is banned upon initial load (if they have `localStorage` login session intact). Because this is an async operation, we'll do it inside the `useEffect` that currently checks `forceLogout`.
Or, we can add a new effect `checkInitialBan` that runs when `isLoggedIn` becomes true.
Wait, `banned_logout` socket event is already emitted in `POST /api/bans` which triggers logout on the client. Why didn't the user logout automatically when banned?
Ah, the memory says: "User ban enforcement operates on two levels: real-time forced logouts via the `banned_logout` Socket.IO event intercepted globally in `HomePage.tsx` (which safely clears local storage and reloads)".
Wait, if `banned_logout` is in `HomePage.tsx`, maybe the socket connection was not open or didn't receive it, or `accountId` mismatch.
Also, the prompt says: "owner ban S ID ban horri but User ID logout nhi hua automatically ... Ban kerne perr Automatically ID logout and User can't Login Jb tk time period Nhi hatanate bss very very strictly".
Wait, in `HomePage.tsx`:
```
    const handleBanLogout = (data: any) => {
      const myId = localStorage.getItem('accountNumber');
      if (myId === data.accountId) {
        localStorage.clear();
        window.location.reload();
      }
    };
```
But `handleBanLogout` doesn't sign out from Firebase. It just clears local storage and reloads.
When it reloads, `onAuthStateChanged` in `app/page.tsx` will still see the Firebase `user` as logged in!
So `localStorage.clear()` is not enough. We also need to do `signOut(auth)`. Wait, `HomePage.tsx` doesn't import `auth`. `onLogout` prop is passed to `HomePage`, which calls `handleLogout` in `page.tsx`. So we should call `onLogout()` instead of just `localStorage.clear()`!

Let's check `HomePage.tsx`.
