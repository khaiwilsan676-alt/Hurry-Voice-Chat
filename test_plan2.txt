Ah, wait. In `HomePage.tsx`:
```
    const handleBanLogout = (data: any) => {
      const myId = localStorage.getItem('accountNumber');
      if (myId === data.accountId) {
        localStorage.clear();
        if (onLogout) {
           onLogout();
        } else {
           window.location.reload();
        }
      }
    };
```
But the prompt also says:
"Ban kerne perr Automatically ID logout and User can't Login Jb tk time period Nhi hatanate bss very very strictly"
"Or Ban kern per User ko niche Login page per Chota Card Bottom Popup hoke Black/10 back card"
"Terx Your Account Has been ban Due to lllegal ect Jo bhi Types hai wo hoga. Unban Time idhr likhdene bss samjhaa"
"Ekdam. Chota card hoga bss or wo login nhi kershkte bss Jb tk Time period naa hate bss"

So what I need to do:
1. In `HomePage.tsx`, modify `handleBanLogout` to check `myId === data.accountId || localStorage.getItem('userUID') === data.userId`. If match, call `onLogout()` so Firebase signs out properly, and then `localStorage.clear()` or maybe `localStorage.setItem('banMessage', ...)` so that after reload / return to login page, the ban message shows up!
2. Actually, if we call `onLogout()`, `app/page.tsx` sets `isLoggedIn` to `false`, throwing the user to `LoginPage`.
In `app/page.tsx`, `handleLogout` does NOT reload the page. It just updates state! So we can pass a `banNotification` from `HomePage` up, or just store it in `localStorage` and read it in `LoginPage`. Or, even better, `app/page.tsx` should also enforce checking ban status on load using API.

Let's modify `components/LoginPage.tsx` so the UI for the popup looks exactly like what is requested.
Wait, "Black/10 back card" means `bg-black/10` or similar? Maybe `bg-black/90` or `bg-black/80`.
The current ban message in `LoginPage.tsx`:
```tsx
      {banMessage && (
        <div className="fixed bottom-10 left-1/2 -translate-x-1/2 bg-black/80 text-white text-sm px-6 py-3 rounded-full text-center max-w-[90%] shadow-lg z-50 whitespace-pre-wrap flex flex-col gap-2">
          {banMessage}
          <button
            onClick={() => setBanMessage(null)}
            className="text-xs bg-white/20 px-3 py-1 rounded-full w-fit mx-auto"
          >
            Close
          </button>
        </div>
      )}
```

But the prompt says:
"Black/10 back card" -> `bg-black/90` or `bg-black/80` is okay.
Text: `Your Account Has been ban Due to [type]. Unban time: [time]`.

Wait, in `ownerban.tsx`:
`accountId: targetUser.accountId, userId: targetUser.id || targetUser.uid`

Wait, `myId === data.accountId` inside `HomePage.tsx` might not match if `accountId` isn't saved as a string or if it's missing.
Also, if the owner bans by `userId`, we need to check that too.
