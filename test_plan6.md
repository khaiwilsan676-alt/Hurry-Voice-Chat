1. Add `deductCoinsFromDB` function to `components/Wallet.tsx` so we can deduct coins when the user buys from the store. Also update `StorePage.tsx` to handle the `buy` logic using this new function. The price is currently stored as string like "500,000", so we need to parse it to a number first.

2. In `components/StorePage.tsx`, update the `handleBuy` logic: when the user clicks 'Buy', deduct the `item.price` (parsed to a number) using `deductCoinsFromDB`. We will import this function from `Wallet.tsx`. Save the purchased item to IndexedDB so it shows in the 'Bag' view. Add `equipItem` and `getEquippedItem` functions to IndexedDB helper in `StorePage.tsx` (or maybe a new file `src/lib/storeDB.ts`) to keep track of equipped items. When the user clicks 'Equip', set it as equipped in `storeDB` and update state. Let's make sure it updates in localstorage as well, like `localStorage.setItem('equippedBubble', itemId)` or similar, or directly in indexedDB. Wait, `localStorage` is easier for sync, we can just save it to `localStorage` under `equipped_EntryEffect`, `equipped_AvatarFrame`, `equipped_Theme`, `equipped_ChatBubble`.

3. For `Entry Effect`: Create a new file `components/EntryEffect.tsx`. This component will check `localStorage.getItem('equipped_Vehicle')` (as entry effects are labeled "Vehicle" in StorePage) and display the corresponding entry card/animation when the component mounts. In `RoomPage.tsx`, we render `<EntryEffect user={currentUser} />` when a user enters. Wait, the prompt says "Hm Jo bhi Entry, frame, Bubble and all Ect Jo bhi Store s Buy kerrengy ouski Value Coins Jo hai hmare wallet Coins  s Cut jongy bss... Also Create Entry Effect. tsx file isme Room K Andar Jb hm Enter kerrengy Room k andar User enter Card Show hua Tb Hmne Jo bhi entry Store s li hai Wohi Entry Room m enter kerte time show hogii bss Or bss Kerro ye... Also Chat bubble image Jo bhi hm lengy wo Chat Bubble Hm Jo bhi msg Room m kerrengy hmare Background Chat Ka card Ouss Image ka hogaa Bss Wide and Hight Bubble khud Set kerlenga bss".
Since the entry animation should trigger on room entry, we'll need to show it via `EntryEffect.tsx`.

4. For `Chat Bubble`: In `RoomPage.tsx` (or `MessagePage.tsx` / `ChatScreen.tsx`), when a message is rendered, we will use the user's equipped chat bubble image as the background for their message. Since this is a real-time app, ideally the user's equipped items are sent along with the message or fetched from their profile, but if it's just for the local user, we can attach the equipped item ID in `socket.emit('room_message', {...msg, bubbleId: ...})` and then look up the bubble image in the receiver's app. Actually, if we just store the items in `localStorage`, we can attach `equippedBubble: localStorage.getItem('equipped_ChatBubble')` to the socket event payload in `sendMessageToSocket` inside `RoomPage.tsx`. In `RoomPage.tsx`'s chat view, if `msg.equippedBubble` exists, render that image as the background, stretching it with CSS (e.g. `border-image` or `background-size: 100% 100%`). The same for `Entry Effect` ("Vehicle").

Let me refine this plan and put it into `set_plan`.
